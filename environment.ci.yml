name: mapie-ci
channels:
    - defaults
    - conda-forge
dependencies:
    - codecov
    - flake8==4.0.1
    - mypy
    - pytest
    - pytest-cov
    - pytest-mock
    - twine
    - wheel
    - pip:
        - -e .
    - pandas
